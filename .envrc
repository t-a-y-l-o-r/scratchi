# Automatically activate uv virtual environment
# Create venv with uv if it doesn't exist, then source it

if [ ! -d .venv ]; then
  echo "Creating uv virtual environment..."
  uv venv
fi

source .venv/bin/activate
